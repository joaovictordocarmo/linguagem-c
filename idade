// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int idade;
    printf("entre com sua idade:");
    scanf("%d", &idade);
    printf("voce tem %d anos \n", idade);
    return 0;
}
